<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MyFinanceApp.MainPage">

    <Grid RowDefinitions="2*,6*,2*">

        <VerticalStackLayout Grid.Row="0" Padding="20" Spacing="10">
            <Label Text="Мои финансы"
                   FontSize="28"
                   HorizontalOptions="Center" />

            <Label Text="25 000 ₽"
                   FontSize="24"
                   TextColor="Green"
                   HorizontalOptions="Center" />

            <ProgressBar Progress="0.6" />
        </VerticalStackLayout>

        <CollectionView Grid.Row="1">
            <CollectionView.ItemsSource>
                <x:Array Type="{x:Type x:String}">
                    <x:String>🍔 Еда - 1200 ₽ - 25.03</x:String>
                    <x:String>🚕 Такси - 500 ₽ - 24.03</x:String>
                    <x:String>💰 Зарплата +30000 ₽ - 23.03</x:String>
                    <x:String>🎮 Игры - 2000 ₽ - 22.03</x:String>
                    <x:String>🛒 Магазин - 1500 ₽ - 21.03</x:String>
                </x:Array>
            </CollectionView.ItemsSource>

            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Border Stroke="Gray" StrokeThickness="1" Margin="10" Padding="10">
                        <Label Text="{Binding}" FontSize="16"/>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <HorizontalStackLayout Grid.Row="2"
                               Padding="10"
                               Spacing="10"
                               HorizontalOptions="Center">

            <Button Text="Доход" Clicked="OnAddClicked"/>
            <Button Text="Расход" Clicked="OnAddClicked"/>
            <Button Text="Отчеты" Clicked="OnReportsClicked"/>

        </HorizontalStackLayout>

    </Grid>

</ContentPage>
